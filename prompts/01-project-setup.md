# Prompt 01: project setup

> Create `frontend/` for Vue and `backend/` for Python/FastAPI. Add concise setup instructions, project-specific agent rules, design and verification notes, a prompts folder, and a current handoff. Use CHECK → TAKE ACTION → VERIFY for dependencies. Keep all work inside the Expedia Lite project.

Outcome: established the two-layer project scaffold and persistent project context required for both assignment parts.
