# Prompt 06: Part 2 persistence verification

> Review every changed file in VS Code Source Control for request-path CSV reads, unconditional seeding, and hardcoded booking IDs. Run backend tests and the frontend production build, then operate the application in a browser: verify hotel and city searches and the no-results state; create, read, cancel, and delete bookings through Vue; verify persistence after refresh and after fully restarting both processes; confirm starter rows are not duplicated; inspect the browser console; and capture full-page JPEG evidence at each required lifecycle state.

Outcome: completed the source review and 11-step SmokeTest, recorded exact database counts and process-restart results, and saved four legible Part 2 screenshots.
