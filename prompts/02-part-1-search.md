# Prompt 02: Part 1 CSV search

> Implement Part 1 of Expedia Lite. The Python backend must read `hotels.csv` and `trips.csv`, join their rows through `hotel_id`, and expose a case-insensitive city search through FastAPI. The Vue frontend must provide a city input, Search button, clearly labeled results table, and clear no-results state. Add focused tests, run the browser SmokeTest for Boston and Miami, and record actual verification results.

Outcome: implemented the Part 1 search flow without adding Part 2 persistence or booking behavior.
